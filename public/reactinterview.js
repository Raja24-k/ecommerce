/**
 *  In react ,a component is a reuseable builiding block for creacting uesr interface
 *  state refer to current data of the component
 * stateful or state management means,when users performs some actions on the UI
 * then react application should be able to update and re-render that data or state on the UI
 * props are way to pass data  from parent to child component
 * ReactDom is javascript library that renders components to DOM browser
 * In react a fragement is way to group multiple children element and 
 * using fragment prevents the addtion of unnecessary dom to components
 * props drilling is the process of passing down props  through multiple
 * layer componts 
 */